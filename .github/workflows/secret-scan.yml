name: secret-scan
on:
  push:
    branches: [main, master]
  pull_request:
jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: 运行密钥扫描
        run: bash scripts/secret-scan.sh
